- if user_signed_in?
  .actions
    .row
      .col-xs-6.like
        = render 'likes/form', likeable: resource
      .col-xs-6.comment
        = link_to resource, class: 'btn btn-block' do
          = fa_icon 'comment'
          comment
